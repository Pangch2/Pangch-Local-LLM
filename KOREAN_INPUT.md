# 한국어 대화·사건·분야 활성화·작업 기억

외부 LLM이나 추가 패키지 없이 실행하는 시제품입니다. 예문의 빈도로
조사→역할, 서술어→동작·시제·부정, 동작→분야를 학습합니다.
선택된 한 분야에서만 그래프를 전파하고 상태 연산과 기억 조회를 실행합니다.
신경망 학습이나 범용 한국어 추론 모델은 아닙니다.

```powershell
python concept_graph_llm_prototype.py --debug
python concept_graph_llm_prototype.py
python korean_dialogue.py korean_dialogues.jsonl korean_dialogue_model.json
python concept_graph_llm_prototype.py --dialogue-model korean_dialogue_model.json
python korean_event_input.py korean_events.jsonl korean_input_model.json
python concept_graph_llm_prototype.py --model korean_input_model.json
python concept_graph_llm_prototype.py --disable-domain 소유
python concept_graph_llm_prototype.py --disable-domain 대화
python concept_graph_llm_prototype.py --disable gravity
python -m unittest discover -s test -v
```

한 줄에 여러 문장을 마침표로 나누어 입력하거나, 여러 차례 이어서 입력할 수 있습니다.

```text
입력: 철수가 책을 가지고 있다. 철수가 영희에게 책을 주었다.
활성 분야: 소유
... 소유 기억 갱신: 책 → 영희
입력: 책을 가진 사람은 누구인가?
책의 소유자: 영희
  근거: 철수가 책을 가지고 있다.
  근거: 철수가 영희에게 책을 주었다.
입력: 영희가 학교에 갔다.
활성 분야: 위치
... 위치 기억 갱신: 영희 → 학교
입력: 영희는 어디에 있나?
영희의 위치: 학교
```

`/초기화`는 이번 세션의 사건·상태 기억을 삭제합니다. `/종료`, 빈 입력,
EOF로 종료합니다. 기억은 세션 안에만 있으며 모델 파일에는 저장하지 않습니다.

## 기본 대화와 문맥

기본 실행은 답변·근거만 표시합니다. `--debug`를 붙이면 한국어 의도,
사건 역할, 활성 분야, 개념 점수도 표시합니다.

```text
입력: 안녕?
안녕하세요! 반가워요. 무엇을 이야기해 볼까요?
입력: 제 이름은 윤서입니다.
윤서님, 반가워요. 이 대화에서 이름을 기억할게요.
입력: 내 이름 기억해?
이 대화에서 알려 주신 이름은 윤서님이에요.
입력: 윤서가 태오에게 지도를 주었다. 태오가 서점에 갔다.
소유 기억 갱신: 지도 → 태오
위치 기억 갱신: 태오 → 서점
입력: 그건 누가 가지고 있어?
지도의 소유자: 태오
입력: 그 사람은 어디에 있어?
태오의 위치: 서점
입력: 왜 그렇게 생각해?
이전에 입력된 사건을 적용해 판단했어요.
  근거: 태오가 서점에 갔다.
```

인사·감사·작별·사과·안부·도움말·자기소개·짧은 감정 표현에 응답합니다.
작별 인사는 프로그램을 종료하지 않으며 `/종료`로 끝냅니다.
이름 소개는 `제 이름은 윤서입니다`, `나는 윤서야`처럼 한 이름을 밝히는
제한된 문형입니다. 이름과 대화 문맥은 `/초기화` 때 함께 삭제됩니다.

후속 소유 질문은 마지막 물건, 후속 위치 질문은 마지막 위치 주체 또는
마지막 소유 갱신/답변의 소유자를 이어받습니다. 문맥이 없으면 대상을
확인합니다. 여러 후보를 비교하는 일반적인 대명사 해석은 아닙니다.
근거 질문은 마지막으로 성공한 상태 답변의 근거를 보여 줍니다. 사건을
새로 처리하거나 다른 상태 질문이 실패하면 이전 답변 근거를 재사용하지 않습니다.

대화 의도는 희소 문자 2·3-gram 역색인으로 관련 예문만 비교해 선택합니다.
최고 Dice 유사도가 0.60 미만이거나 서로 다른 의도의 점수 차이가 0.08 미만이면
보류합니다. 이 점수는 확률이나 신뢰도 보정값이 아닙니다. 명시적인 상태 질문이
우선이며, 인사 판별은 미지원 물음표 입력을 거절하기 전에 실행됩니다.
‘안녕이라는 말의 뜻은?’ 같은 부정 예문은 단어 포함만으로 인사하지 않게 합니다.

대화에는 `대화` 분야만, 후속 소유·위치 질문에는 해당 상태 분야만 활성화합니다.
대화 모델은 언어 입력을 분류하는 공통 입구이며 소유·위치 기억은 읽지 않습니다.
`--disable-domain 대화`는 인사·이름 기억 등의 대화 응답을 차단하고,
`--disable-domain 위치`는 직접/후속 위치 질문과 해당 근거 조회를 모두 차단합니다.

## 대화 데이터와 학습 범위

`korean_dialogues.jsonl`에는 17개 라벨의 학습 예문 146개가 있습니다.
각 행은 `text`, `intent`, `response`로 구성합니다. `기타`는 처리 대상이 아닌
문장을 구별하는 라벨이며 별도 대화 응답을 생성하지 않습니다.
이름 응답에만 `{이름}`을 치환하며 그 밖의 임의 코드나 템플릿은 실행하지 않습니다.

`korean_dialogue_eval.jsonl`은 학습에 넣지 않는 개발용 표현 24개입니다.
현재 20개가 기대 라벨과 일치하고 4개는 보류합니다. 이 개발 자료는 모델 개선에
참고한 작은 수작업 자료이며 독립적인 범용 한국어 성능 지표가 아닙니다.
테스트는 자신 있게 다른 의도로 분류하는 오류와 판단 보류를 구분합니다.
기존 20개 추론 대화·54개 입력도 대화 분류기를 켠 상태로 다시 검사합니다.

데이터를 수정하면 대화 학습 명령을 다시 실행하세요. 기본 실행은 제공된 예문을
메모리에서 학습하며 `--dialogue-model`로 저장 모델을 사용할 수 있습니다.
저장 모델은 라벨이 붙은 예문과 응답을 저장하고 읽을 때 역색인을 재구성합니다.

현재 ‘한국어로 생각하기’는 한국어 의도·역할·대상·근거를 보존하고 연산하는
제한된 구조를 뜻합니다. 문맥 연결·상태 변경은 명시적 규칙이고, 대화 응답은
가장 가까운 학습 예문의 문구와 확인된 기억을 조합합니다. 자유로운 문장 생성,
일반 상식 질의응답, 대규모 신경망 학습은 아직 구현하지 않았습니다.

## 학습 자료와 검증 자료

JSONL의 각 줄은 한 문장의 정답입니다. `surface`는 문장 끝 서술어 표현,
`predicate`는 기본 동작, `tense`는 과거/현재/미래, `negated`는 부정 여부입니다.
역할의 `surface`는 조사 포함 어절, `value`는 조사 제외 명사입니다.
`domain`은 소유/위치/물리/식사 등의 분야 이름입니다. 생략한 기존 예문도
입력 학습에 사용되지만, 분야를 선택하려면 해당 동작의 분야 주석이 필요합니다.
동일 동작의 분야 빈도가 동률이면 임의로 선택하지 않습니다.

`korean_events.jsonl`의 기존 예문을 보존하고 59개를 추가해 총 139개가 되었습니다.
‘받다’의 상대방 주석 3개는 ‘주는사람’으로 수정했습니다.
`korean_reasoning_eval.jsonl`은 학습에 사용하지 않는 20개 대화 시나리오입니다.
새 이름·대상·어순, 연속 이전, 교차 분야, 부정·미래, 모순, 근거 부족,
분야/동작 차단을 검사합니다. 직접 만든 제한된 합성 자료이며 일반 한국어
벤치마크가 아닙니다. 검증 통과를 범용 추론 성능으로 해석하면 안 됩니다.

자료를 수정한 뒤 학습 명령을 다시 실행하면 새 모델이 저장됩니다.
모델을 지정하지 않으면 학습 자료로 메모리에서 학습합니다. 모델 버전 2는
분야 빈도도 저장합니다. 버전 1은 불러올 수 있지만 분야 주석으로 재학습해야 합니다.

## 구조와 현재 연산

의도/사건 분석 → 문맥 대상 확인 → 분야 선택 → 해당 분야 그래프 활성화 → 대화 응답 또는 상태 변경/조회

`KoreanEvent`의 역할·시제·부정을 세션에 보존합니다. 추론 연산은 개념 시드
목록이 아닌 사건 전체를 읽습니다. 같은 인물·물체가 등장해도 역할이 바뀌면
결과가 달라집니다. 장기 개념 그래프에 개별 사건을 사실로 자동 저장하지 않습니다.

| 분야 | 연산 | 상태 |
|---|---|---|
| 소유 | 가지다 | 대상의 소유자 = 행위자 |
| 소유 | 주다 | 대상의 소유자 = 받는사람 |
| 소유 | 받다 | 대상의 소유자 = 행위자; 주는사람 별도 확인 |
| 위치 | 가다·오다·이동하다·있다 | 행위자의 위치 = 장소 |

연산 의미와 질문 패턴은 현재 명시적인 규칙입니다. 분야 분류와 입력 표현만
학습하며 연산 자체를 정답 보상으로 학습하지는 않습니다. 물리·식사는 개념을
활성화하고 사건을 보존하지만 현재 상태 변경 연산이나 질문 응답은 없습니다.

소유 이전 시 기존 소유자가 알려져 있으면 주는 사람과 일치해야 합니다.
모순, 필수 역할 누락, 해석되지 않은 어절은 상태 변경을 보류합니다.
부정·미래 사건은 기록만 하고 현재 상태를 바꾸지 않습니다. 이는 부정 사실을
완전히 추론하는 기능이 아니며 기존 긍정 사실의 철회도 아직 처리하지 않습니다.
답변에는 실제 상태 갱신에 사용한 문장을 근거로 출력합니다.

## 분야 활성화 보장과 한계

부정문에서도 언급된 개념은 활성화되지만 사건이 참이라고 단정하지 않습니다.
등록되지 않은 이름은 사건에는 남고 그래프 시드에는 포함되지 않습니다.
작업 기억은 이름을 직접 키로 사용하므로 새로운 인물·물체도 기록할 수 있습니다.

노드의 `domains`가 비어 있으면 공유 개념입니다. 분야가 붙은 노드는 해당 분야가
선택되어야 시드·전파에 참여합니다. 공유 개념에서 다른 분야로 연결되어 있어도
분야 경계에서 차단합니다. 새 그래프 지식을 추가할 때 분야 주석도 지정해야 합니다.
`--disable-domain`은 분야 연산·기억 조회를 차단하고 `--disable`은 개별 개념을
차단합니다. 둘 다 반복 지정할 수 있습니다. 다음 입력에서는 분야를 다시 선택하고
이전 분야의 상태만 보존합니다. 비활성 분야의 기억은 조회하지 않습니다.

직접 `ActivationRouter.activate()`를 호출할 때는 `active_domains`를 전달해야
분야 필터가 켜집니다. 생략하면 이전 API와 같이 분야 제한 없이 동작합니다.
top_k는 선택할 메시지를 제한하지만 활성 노드의 인접 엣지를 읽는 비용까지
제한하지 않습니다. 현재 분야 단위는 소유·위치 같은 작은 기능 영역입니다.

문장당 한 사건과 한 어절 명사를 지원합니다. 현재 소유자·위치 질문만 지원하며
생략·피동·복문·동음이의어·일반 질문은 처리하지 않습니다. 동일 이름은 동일
인물/물체 하나로 취급하고 사건은 입력 순서로 적용합니다. 명시적 시간 비교,
서로 다른 두 권의 ‘책’ 구별, 습관과 현재 사건의 구별은 지원하지 않습니다.
